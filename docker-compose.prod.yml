# Overrides para produção — use com:
# docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d

services:
  backend:
    environment:
      - APP_ENV=production
      - APP_DEBUG=false

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
