/**
 * ATENÇÃO: Tailwind v4 ignora este arquivo.
 * Os tokens customizados estão definidos via @theme em src/index.css.
 * Este arquivo é mantido apenas como referência documental dos tokens.
 *
 * @see src/index.css (@theme block)
 * @see src/styles/tokens.css (CSS custom properties)
 */

/** @type {import('tailwindcss').Config} */
export default {
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
  // No Tailwind v4, esta configuração é ignorada.
  // Consulte src/index.css para os tokens @theme ativos.
}
