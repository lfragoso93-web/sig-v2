# Validação CI — audit-python e audit-nodejs

Este arquivo é temporário e será removido após confirmação dos jobs.

## Jobs validados

- `audit-python` — pip-audit em `backend/requirements.txt`
- `audit-nodejs` — npm audit em `frontend/`

Ambos substituem o `dependency-review-action@v5` que exigia GitHub Advanced Security.
