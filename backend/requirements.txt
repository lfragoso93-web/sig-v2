# 2026-06-16 security update
#
# fastapi 0.120.0 declara starlette<0.49.0 - nao aceita o fix do CVE-2025-62727
# fastapi>=0.138.1 usa starlette>=0.46.0 sem upper cap
# starlette>=1.3.1 force o pip a resolver para versao com fix do CVE-2025-62727
fastapi>=0.138.1
starlette>=1.3.1
uvicorn[standard]==0.49.0
sqlalchemy==2.0.51
psycopg2-binary==2.9.12
asyncpg==0.31.0
pydantic==2.13.4
pydantic[email]==2.13.4
# pydantic-settings 2.14.2 - fix GHSA-4xgf-cpjx-pc3j
# NestedSecretsSettingsSource seguia symlinks fora de secrets_dir (leitura arbitraria de arquivos)
pydantic-settings==2.14.2
alembic==1.18.4
httpx==0.27.2
# yfinance 0.2.54 - corrige JSONDecodeError e YFTzMissingError causados por mudancas
# na API interna do Yahoo Finance. Versoes anteriores (<=0.2.40) retornam HTML/vazio
# ao inves de JSON, quebrando o parse. Tambem adiciona suporte a yf.Lookup (Search).
yfinance==0.2.54
# PyJWT 2.13.0 - fix CVE-2026-32597
# python-jose removido: dependencia pyasn1<0.5.0 conflita com fix CVE-2026-30922
# toda logica JWT usa PyJWT diretamente via app.core.security
PyJWT[crypto]==2.13.0
# cryptography 49.0.0 - GHSA-537c-gmf6-5ccf exige >=48.0.1
# OpenSSL estaticamente linkado nas wheels 46.x/47.x estava vulneravel
cryptography==49.0.0
# passlib removido: abandonado e incompativel com bcrypt>=5.0.0
# hash/verify de senha agora usa bcrypt nativo diretamente em app.core.security
bcrypt==5.0.0
# python-multipart 0.0.32 - GHSA-5rvq-cxj2-64vf (CVE-2026-53539)
python-multipart==0.0.32
apscheduler==3.11.2
email-validator==2.2.0
redis==5.2.1
# reportlab: geracao de PDF no modulo IRPF
reportlab==4.2.5
# slowapi: rate limiting por IP para endpoints sensiveis (login, register)
# limits: backend de storage do slowapi (memoria ou Redis)
slowapi>=0.0.7
limits>=3.7.0
